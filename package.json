{
  "name": "signal-path-verify",
  "version": "1.0.0",
  "description": "Static CareerOS employer prototype for verified labour-demand infrastructure.",
  "type": "module",
  "scripts": {
    "start": "npx serve . -l 5189",
    "test": "node tests/verify-engine.test.js && node tests/permissions.test.js && node tests/wiring.test.js",
    "build": "node --check verify-engine.js && node --check verify-seeds.js && node --check verify-store.js && node --check permissions.js && node --check viz.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
